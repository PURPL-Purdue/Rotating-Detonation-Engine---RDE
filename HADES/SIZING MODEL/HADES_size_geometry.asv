function [det_wave_path_length] = HADES_size_geometry(outer_radius, wall_thickness, annulus_gap)

Outer Radius
Wall Thickness
Annulus Gap
Inner Radius
Annulus Radius
Detonation Wave Path Length

% Print Values
fprintf("\nGeometry:\n");
fprintf("Outer Radius: %.3f \n", outer_radius);
fprintf("Wall Thickness: %.3f \n", outer_r);
fprintf("Annulus Gap: %.3f \n", outer_r);
fprintf("Inner Radius: %.3f \n", outer_r);
fprintf("Detonation Wave Path Length: %.3f \n", outer_r);